
{{!-- 
{{#if user.isAdmin}}
    <h2>Admin Profile</h2>
    <p>Welcome, Admin {{user.first_name}} {{user.last_name}}</p>
{{else}}
    <h2>My Profile</h2>
    <p>Welcome, {{user.first_name}} {{user.last_name}}</p>
{{/if}}
<p>Email: {{user.email}}</p>
<p>Age: {{user.age}}</p>
{{!-- <p>Role: {{user.role}}</p> --}}


{{#if user.isAdmin}}
    <h2>Admin Profile</h2>
    <p>Welcome, Admin {{user.first_name}} {{user.last_name}}</p>
{{else}}
    <h2>My Profile</h2>
    <p>Welcome, {{user.first_name}} {{user.last_name}}</p>
{{/if}}
<p>Email: {{user.email}}</p>
<p>Age: {{user.age}}</p>
{{!-- <p>Role: {{user.role}}</p> --}}
<form action="/api/sessions/logout" method="post">
    <button type="submit">Logout</button>
</form>